{
  "name": "@lnd-boilerplate/utils",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "format": "prettier --write src",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "eslint": "^8.57.1",
    "prettier": "^3.6.2"
  }
}
